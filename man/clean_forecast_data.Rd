% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6.Forcast_Table.R
\name{clean_forecast_data}
\alias{clean_forecast_data}
\title{Clean Forecast Data}
\usage{
clean_forecast_data(data, px, model)
}
\arguments{
\item{data}{A data frame containing forecast data.}

\item{px}{The patient ID to filter the data.}

\item{model}{The model type to filter the data. Default is 'ARIMA_mean'.}

\item{ci}{The confidence interval type to filter the data. Default is '95\%'.}
}
\value{
A cleaned and processed data frame with the age in years, expected
BMI, and expected weight, formatted appropriately.
}
\description{
This function cleans and processes forecast data by filtering based on
patient ID, model type, and confidence interval. It formats the data to
include only ages divisible by 6 and rounds all numerical values to one
decimal place. It also formats the expected BMI and expected weight.
}
\examples{
# Assuming 'forecast_data' is a data frame containing the relevant columns
clean_data <- clean_forecast_data(forecast_data, px = 12345)
}
