# Create tsibble for BMIz Data

Create tsibble for BMIz Data

## Usage

``` r
make_bmiz_tsibble(data)
```

## Arguments

- data:

  A data frame containing the input data - cleaned per clean_data
  function

## Value

A tsibble with the BMIz data.
