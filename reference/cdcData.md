# CDC Growth Data

A dataset containing CDC growth data used for calculating BMI and other
growth metrics.

## Usage

``` r
data(cdcData)
```

## Format

An object of class `data.frame` with 438 rows and 35 columns.
