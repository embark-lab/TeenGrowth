# Convert Percentile to Z-Score

Convert Percentile to Z-Score

## Usage

``` r
pct_to_bmiz(pct)
```

## Arguments

- pct:

  Percentile value (0-100).

## Value

Z-score corresponding to the given percentile.
